import PlaceholderDashboard from '../../../components/dashboard/PlaceholderDashboard';

/**
 * SynapsesDashboard — Synapses client dashboard.
 * Phase 2+: Replace PlaceholderDashboard with real charts, KPIs, and tables.
 */
export default function SynapsesDashboard({ clientId }) {
  return <PlaceholderDashboard clientName="Synapses" clientId={clientId} />;
}
