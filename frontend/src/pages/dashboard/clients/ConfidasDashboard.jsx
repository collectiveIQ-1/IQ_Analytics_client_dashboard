import PlaceholderDashboard from '../../../components/dashboard/PlaceholderDashboard';

/**
 * ConfidasDashboard — Confidas client dashboard.
 * Phase 2+: Replace PlaceholderDashboard with real charts, KPIs, and tables.
 */
export default function ConfidasDashboard({ clientId }) {
  return <PlaceholderDashboard clientName="Confidas" clientId={clientId} />;
}
