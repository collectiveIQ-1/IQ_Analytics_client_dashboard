import PlaceholderDashboard from '../../../components/dashboard/PlaceholderDashboard';

/**
 * CompleteNeuroDashboard — Complete Neuro client dashboard.
 * Phase 2+: Replace PlaceholderDashboard with real charts, KPIs, and tables.
 */
export default function CompleteNeuroDashboard({ clientId }) {
  return <PlaceholderDashboard clientName="Complete Neuro" clientId={clientId} />;
}
