import PlaceholderDashboard from '../../../components/dashboard/PlaceholderDashboard';

/**
 * InnervateDashboard — Innervate client dashboard.
 * Phase 2+: Replace PlaceholderDashboard with real charts, KPIs, and tables.
 */
export default function InnervateDashboard({ clientId }) {
  return <PlaceholderDashboard clientName="Innervate" clientId={clientId} />;
}
