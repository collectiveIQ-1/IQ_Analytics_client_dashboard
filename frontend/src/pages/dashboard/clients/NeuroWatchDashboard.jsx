import PlaceholderDashboard from '../../../components/dashboard/PlaceholderDashboard';

/**
 * NeuroWatchDashboard — Neuro Watch client dashboard.
 * Phase 2+: Replace PlaceholderDashboard with real charts, KPIs, and tables.
 */
export default function NeuroWatchDashboard({ clientId }) {
  return <PlaceholderDashboard clientName="Neuro Watch" clientId={clientId} />;
}
