import PlaceholderDashboard from '../../../components/dashboard/PlaceholderDashboard';

/**
 * USNeuroDashboard — USNeuro client dashboard.
 * Phase 2+: Replace PlaceholderDashboard with real charts, KPIs, and tables.
 */
export default function USNeuroDashboard({ clientId }) {
  return <PlaceholderDashboard clientName="USNeuro" clientId={clientId} />;
}
