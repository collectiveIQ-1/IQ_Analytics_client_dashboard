import PlaceholderDashboard from '../../../components/dashboard/PlaceholderDashboard';

/**
 * MindSyncDashboard — Mind Sync client dashboard.
 * Phase 2+: Replace PlaceholderDashboard with real charts, KPIs, and tables.
 */
export default function MindSyncDashboard({ clientId }) {
  return <PlaceholderDashboard clientName="Mind Sync" clientId={clientId} />;
}
