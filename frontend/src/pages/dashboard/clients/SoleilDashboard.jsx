import PlaceholderDashboard from '../../../components/dashboard/PlaceholderDashboard';

/**
 * SoleilDashboard — Soleil Surgery client dashboard.
 * Phase 2+: Replace PlaceholderDashboard with real charts, KPIs, and tables.
 */
export default function SoleilDashboard({ clientId }) {
  return <PlaceholderDashboard clientName="Soleil Surgery" clientId={clientId} />;
}
