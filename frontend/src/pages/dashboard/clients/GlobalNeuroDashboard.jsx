import PlaceholderDashboard from '../../../components/dashboard/PlaceholderDashboard';

/**
 * GlobalNeuroDashboard — Global Neurodiagnostic client dashboard.
 * Phase 2+: Replace PlaceholderDashboard with real charts, KPIs, and tables.
 */
export default function GlobalNeuroDashboard({ clientId }) {
  return <PlaceholderDashboard clientName="Global Neurodiagnostic" clientId={clientId} />;
}
