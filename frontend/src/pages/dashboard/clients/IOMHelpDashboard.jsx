import PlaceholderDashboard from '../../../components/dashboard/PlaceholderDashboard';

/**
 * IOMHelpDashboard — IOM Help client dashboard.
 * Phase 2+: Replace PlaceholderDashboard with real charts, KPIs, and tables.
 */
export default function IOMHelpDashboard({ clientId }) {
  return <PlaceholderDashboard clientName="IOM Help" clientId={clientId} />;
}
